<view class="home-page">
  <!-- 轮播图区域 -->
  <view class="banner-section">
    <swiper 
      class="banner-swiper"
      indicator-dots="{{banners.length > 1}}"
      autoplay="{{banners.length > 1}}"
      interval="{{3000}}"
      duration="{{500}}"
      circular="{{banners.length > 1}}"
      indicator-color="rgba(255,255,255,0.5)"
      indicator-active-color="#ffffff"
    >
      <swiper-item wx:for="{{banners}}" wx:key="_id">
        <view class="banner-item" wx:if="{{item.imageUrl}}">
          <image class="banner-img" src="{{item.imageUrl}}" mode="aspectFill" />
          <view class="banner-overlay">
            <text class="banner-title" wx:if="{{item.title}}">{{item.title}}</text>
            <text class="banner-subtitle" wx:if="{{item.subtitle}}">{{item.subtitle}}</text>
          </view>
        </view>
        <view class="banner-item" wx:else style="background: linear-gradient(135deg, {{item.color1 || '#c8a97e'}}, {{item.color2 || '#b8986e'}})">
          <text class="banner-icon">{{item.icon || '📸'}}</text>
          <text class="banner-title">{{item.title || '专业COS摄影'}}</text>
          <text class="banner-subtitle">{{item.subtitle || '还原你心中的角色'}}</text>
        </view>
      </swiper-item>
    </swiper>
  </view>

  <!-- 欢迎区域 -->
  <view class="welcome-section">
    <view class="welcome-card">
      <text class="welcome-title">欢迎和小锅拍照</text>
      <text class="welcome-sub">让我们度过快乐的时光</text>
    </view>
  </view>

  <!-- 快捷入口 -->
  <view class="quick-actions">
    <view class="action-item" bindtap="goToBooking">
      <view class="action-icon action-book">📅</view>
      <text class="action-label">我要预约</text>
    </view>
    <view class="action-item" bindtap="goToOrders">
      <view class="action-icon action-orders">📋</view>
      <text class="action-label">我的订单</text>
    </view>
    <view class="action-item" bindtap="goToGallery">
      <view class="action-icon action-gallery">🖼️</view>
      <text class="action-label">更多例图</text>
    </view>
  </view>

  <!-- 瀑布流作品展示 -->
  <view class="waterfall-section">
    <view class="section-header">
      <text class="section-title">作品展示</text>
    </view>
    
    <view wx:if="{{loading}}" class="loading-tip">
      <text>加载中...</text>
    </view>
    
    <view wx:elif="{{waterfallPhotos.length === 0}}" class="empty-tip">
      <text>暂无作品</text>
    </view>
    
    <view wx:else class="waterfall-container">
      <view class="waterfall-column">
        <view 
          wx:for="{{waterfallPhotos}}" 
          wx:key="id"
          wx:if="{{index % 2 === 0}}"
          class="waterfall-item"
          bindtap="previewPhoto"
          data-url="{{item.url}}"
        >
          <image 
            class="waterfall-img" 
            src="{{item.url}}" 
            mode="widthFix"
            lazy-load
          />
          <view class="waterfall-info" wx:if="{{item.role}}">
            <text class="waterfall-role">{{item.role}}</text>
          </view>
          <view class="featured-tag" wx:if="{{item.isFeatured}}">精选</view>
        </view>
      </view>
      
      <view class="waterfall-column">
        <view 
          wx:for="{{waterfallPhotos}}" 
          wx:key="id"
          wx:if="{{index % 2 === 1}}"
          class="waterfall-item"
          bindtap="previewPhoto"
          data-url="{{item.url}}"
        >
          <image 
            class="waterfall-img" 
            src="{{item.url}}" 
            mode="widthFix"
            lazy-load
          />
          <view class="waterfall-info" wx:if="{{item.role}}">
            <text class="waterfall-role">{{item.role}}</text>
          </view>
          <view class="featured-tag" wx:if="{{item.isFeatured}}">精选</view>
        </view>
      </view>
    </view>
  </view>
</view>
